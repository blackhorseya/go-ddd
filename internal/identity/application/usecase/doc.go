// Package usecase contains application use cases for the Identity bounded context.
//
// Each use case orchestrates domain logic to fulfill a single business operation.
// Use cases accept and return DTOs, never domain objects directly.
//
// Naming convention: {verb}_{noun}.go (e.g. register_user.go, activate_user.go)
package usecase
